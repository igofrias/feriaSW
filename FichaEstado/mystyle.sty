\ProvidesPackage{mystyle}

\usepackage[latin1]{inputenc} %codificacion de caracteres
\usepackage[spanish,es-tabla]{babel} %idioma, con configuracion para tablas en ES
\usepackage{fancyhdr} %tipo de pagina, LINDA biggrin.gif
\usepackage[top=2.5cm,bottom=2.5cm,right=2cm,left=2.5cm]{geometry} %margenes
\usepackage[rflt]{floatflt} %ni puta idea
\usepackage{pdfpages} %incluir archivos pdf
\usepackage{float}    %edicion de elementos flotantes
\usepackage{appendix} %configuraciones del apendice
\usepackage{hyperref} %hipervinculos
\hypersetup{
  colorlinks=true,
  urlcolor=blue,
  linkcolor=blue,
  citecolor=blue
}
%\usepackage{helvet} %esto es pa escribir con Arial en vez de times new roman
%\renewcommand\familydefault{\sfdefault} %descomenta estas lineas para escribir en arial

\usepackage{ragged2e}%permite el uso de \justifying

\usepackage{array}    %agrega funcionalidades en tablas
\usepackage{multirow} %multicolumnas en tablas

\usepackage{graphicx} %para usar imagenes
\usepackage{caption}
\usepackage{subcaption}

\usepackage{emptypage}%elimina el formato de las paginas en blanco

\graphicspath{{./pic/}} %le dice que las imagenes estan en la carpeta de arriba

\usepackage{amsmath, amsthm, amsfonts} %pa usar smbolos matematicos
\numberwithin{equation}{section} % pa usar ecuaciones de modo lindo
\numberwithin{figure}{section} %para agregar imagenes
\numberwithin{table}{section} %para agregar tablas

\usepackage{chngcntr}
\counterwithin{equation}{section}
\counterwithin{figure}{section}
\counterwithin{table}{section}

\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

\addto\captionsspanish{\renewcommand\chaptername{Parte}}
\addto\captionsspanish{\renewcommand\appendixname{Anexos}}
\renewcommand\appendixtocname{Anexos}
\renewcommand\appendixpagename{Anexos}

% Teoremas
%--------------------------------------------------------------------------
\newtheorem{thm}{Teorema}[section]
\newtheorem{cor}[thm]{Corolario}
\newtheorem{lem}[thm]{Lema}
\newtheorem{prop}[thm]{Proposición}
\theoremstyle{definition}
\newtheorem{defn}[thm]{Definición}
\theoremstyle{remark}
\newtheorem{rem}[thm]{Observación}

% Atajos.
% Se pueden definir comandos nuevos para acortar cosas que se usan
% frecuentemente. Como ejemplo, aquí se definen la R y la Z dobles que
% suelen representar a los conjuntos de números reales y enteros.
%--------------------------------------------------------------------------

\def\RR{\mathbb{R}}
\def\ZZ{\mathbb{Z}}

% De la misma forma se pueden definir comandos con argumentos. Por
% ejemplo, aquí definimos un comando para escribir el valor absoluto
% de algo más fácilmente.
%--------------------------------------------------------------------------
\newcommand{\abs}[1]{\left\vert#1\right\vert}

% Operadores.
% Los operadores nuevos deben definirse como tales para que aparezcan
% correctamente. Como ejemplo definimos en jacobiano:
%--------------------------------------------------------------------------
\DeclareMathOperator{\Jac}{Jac}

%--------------------------------------------------------------------------
\pagestyle{fancy} %Configuracion para la pagina linda
\fancyhf{}
%\renewcommand{\chaptermark}[1]{\markboth{\MakeUppercase{\chaptername~\thechapter. #1}}{}}
%\renewcommand{\sectionmark}[1]{\markright{\textbf{\thesection. #1}}}

% primera pagina de un capitulo
\fancypagestyle{plain}{
  \fancyhead[L]{}
  \fancyhead[C]{}
  \fancyhead[R]{}
  \fancyfoot[L]{}
  \fancyfoot[C]{}
  \fancyfoot[R]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

\fancyhead[L]{\includegraphics[scale=0.2]{logo_Phyrex8.png}}%el logo se coloca en cabecera. Pagina par a la derecha y pagina impar a la izquierda
\fancyhead[CO,CE]{\includegraphics[scale=0.25]{logoUSM-DI.eps}}
\fancyhead[R]{\includegraphics[scale=0.07]{VIPeR.png}}%Capitulos se colocan en encabezado. Paginas pares a la izquierda
\fancyhead[RO]{\rightmark}%Secciones se colocan en encabezado. Paginas impares a la derecha
\fancyfoot[LE,RO]{\thepage}%Numeros de pagina en pie de pagina. Pagina par a la izquierda. Pagina impar a la derecha.
\renewcommand{\headrulewidth}{0.5pt}

\setlength{\headheight}{53pt} %alto del encabezado
%--------------------------------------------------------------------------
